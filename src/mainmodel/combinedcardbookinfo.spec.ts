import { Combinedcardbookinfo } from './combinedcardbookinfo';

describe('Combinedcardbookinfo', () => {
  it('should create an instance', () => {
    expect(new Combinedcardbookinfo()).toBeTruthy();
  });
});
