import { Compositeratingclass } from './compositeratingclass';

describe('Compositeratingclass', () => {
  it('should create an instance', () => {
    expect(new Compositeratingclass()).toBeTruthy();
  });
});
